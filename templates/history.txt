<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>History - Smart Invoice Analyzer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <!-- Navbar -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <i class="fas fa-file-invoice text-blue-600 text-2xl mr-3"></i>
                    <span class="text-xl font-bold text-gray-800">Smart Invoice Analyzer</span>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="{{ url_for('dashboard') }}" class="text-blue-600 hover:text-blue-800">
                        <i class="fas fa-home mr-1"></i> Dashboard
                    </a>
                    <span class="text-gray-600">{{ current_user.username }}</span>
                    <a href="{{ url_for('logout') }}" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                        <i class="fas fa-sign-out-alt mr-1"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="bg-white rounded-lg shadow-md p-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-8">
                <i class="fas fa-history text-blue-600 mr-3"></i>Document History
            </h1>

            {% if documents.items %}
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-100">
                        <tr>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Date</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">File</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Type</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Vendor</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Amount</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Confidence</th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                        {% for doc in documents.items %}
                        <tr class="hover:bg-gray-50 transition">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="text-sm text-gray-900">{{ doc.upload_date.strftime('%Y-%m-%d') }}</div>
                                <div class="text-xs text-gray-500">{{ doc.upload_date.strftime('%H:%M:%S') }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-gray-900 truncate max-w-xs">{{ doc.filename }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <span class="px-3 py-1 text-xs font-bold rounded-full 
                                    {% if doc.document_type == 'invoice' %}bg-blue-100 text-blue-800
                                    {% elif doc.document_type == 'receipt' %}bg-green-100 text-green-800
                                    {% else %}bg-purple-100 text-purple-800{% endif %}">
                                    {{ doc.document_type|upper }}
                                </span>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-gray-900 truncate max-w-xs">{{ doc.vendor_name }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm font-bold text-gray-900">{{ doc.currency }} {{ "%.2f"|format(doc.total_amount) }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex items-center">
                                    <div class="w-24 bg-gray-200 rounded-full h-2 mr-2">
                                        <div class="bg-gradie    style="width: {{ (doc.confidence_score * 100)|int }}%"></div>nt-to-r from-green-400 to-green-600 h-2 rounded-full" 
                                         
                                    </div>
                                    <span class="text-sm font-medium">{{ (doc.confidence_score * 100)|int }}%</span>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex space-x-2">
                                    <button onclick="viewDocument({{ doc.id }})" 
                                            class="text-blue-600 hover:text-blue-800 transition">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                    <button onclick="deleteDocument({{ doc.id }})" 
                                            class="text-red-600 hover:text-red-800 transition">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <div class="mt-8 flex justify-center space-x-2">
                {% if documents.has_prev %}
                    <a href="{{ url_for('history', page=documents.prev_num) }}" 
                       class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
                        <i class="fas fa-chevron-left"></i> Previous
                    </a>
                {% endif %}
                
                <span class="px-4 py-2 bg-gray-200 rounded">
                    Page {{ documents.page }} of {{ documents.pages }}
                </span>
                
                {% if documents.has_next %}
                    <a href="{{ url_for('history', page=documents.next_num) }}" 
                       class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
                        Next <i class="fas fa-chevron-right"></i>
                    </a>
                {% endif %}
            </div>

            {% else %}
            <div class="text-center py-12">
                <i class="fas fa-inbox text-6xl text-gray-300 mb-4"></i>
                <p class="text-xl text-gray-600">No documents found</p>
                <a href="{{ url_for('dashboard') }}" 
                   class="mt-4 inline-block bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700">
                    Upload Your First Document
                </a>
            </div>
            {% endif %}
        </div>
    </div>

    <!-- View Modal -->
    <div id="viewModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg p-8 max-w-2xl w-full mx-4 max-h-screen overflow-y-auto">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800">Document Details</h2>
                <button onclick="closeModal()" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times text-2xl"></i>
                </button>
            </div>
            <div id="modalContent"></div>
        </div>
    </div>

    <script>
        function viewDocument(id) {
            fetch(`/document/${id}`)
                .then(response => response.json())
                .then(data => {
                    document.getElementById('modalContent').innerHTML = `
                        <div class="space-y-4">
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <p class="text-sm text-gray-600">Filename</p>
                                    <p class="font-bold">${data.filename}</p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">Upload Date</p>
                                    <p class="font-bold">${data.upload_date}</p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">Document Type</p>
                                    <p class="font-bold">${data.document_type.toUpperCase()}</p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">Confidence Score</p>
                                    <p class="font-bold">${(data.confidence * 100).toFixed(0)}%</p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">Vendor</p>
                                    <p class="font-bold">${data.vendor_name}</p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">Amount</p>
                                    <p class="font-bold text-green-600 text-xl">${data.currency} ${data.total_amount.toFixed(2)}</p>
                                </div>
                            </div>
                            <div>
                                <p class="text-sm text-gray-600 mb-2">Extracted Text</p>
                                <div class="bg-gray-100 p-4 rounded max-h-64 overflow-y-auto">
                                    <pre class="text-sm whitespace-pre-wrap">${data.text}</pre>
                                </div>
                            </div>
                        </div>
                    `;
                    document.getElementById('viewModal').classList.remove('hidden');
                })
                .catch(error => alert('Error loading document: ' + error));
        }

        function closeModal() {
            document.getElementById('viewModal').classList.add('hidden');
        }

        function deleteDocument(id) {
            if (confirm('Are you sure you want to delete this document?')) {fetch(`/delete/${id}`, { method: 'POST' })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        location.reload();
                    } else {
                        alert('Error deleting document');
                    }
                })
                .catch(error => alert('Error: ' + error));
        }
    }
</script>